{
  "name": "gosoki-portfolio",
  "version": "1.0.0",
  "description": "Gosoki's Portfolio",
  "private": true,
  "scripts": {
    "start": "eleventy --serve --port=8233",
    "build": "npm run clean && ELEVENTY_ENV=production eleventy",
    "clean": "rm -rf _site",
    "check-links": "node tools/check-links.js",
    "subset-fonts": "node tools/subset-fonts.js",
    "mark-links": "node tools/check-links.js --embeds --mark"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
